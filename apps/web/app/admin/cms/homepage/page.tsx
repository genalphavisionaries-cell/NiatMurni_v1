import { CmsHomepageClient } from "./CmsHomepageClient";

export const metadata = {
  title: "Homepage | CMS | Admin | Niat Murni",
};

export default function AdminCmsHomepagePage() {
  return <CmsHomepageClient />;
}
